<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Smart Fishery Management System</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    :root {
      --bg-color: #e4f2ff;
      --primary-color: #2a97c9;
      --secondary-color: #0e5c85;
      --accent-color: #00ffcc;
      --text-color: #1a3b5b;
      --shadow-light: #f5fbff;
      --shadow-dark: #ccdae9;
    }
    
    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg-color);
      min-height: 100vh;
      width: 100%;
      overflow: hidden;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    /* Background pattern */
    .background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      opacity: 0.3;
      z-index: -1;
      overflow: hidden;
    }
    
    .background::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 20%, var(--primary-color) 0%, transparent 40%),
        radial-gradient(circle at 80% 80%, var(--secondary-color) 0%, transparent 40%),
        radial-gradient(circle at 50% 50%, var(--accent-color) 0%, transparent 25%);
      filter: blur(60px);
    }
    
    /* Water pattern */
    .pattern {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 35%;
      background-image: 
        repeating-radial-gradient(
          circle at center center, 
          transparent 0px, 
          transparent 13px, 
          rgba(255, 255, 255, 0.06) 13px, 
          rgba(255, 255, 255, 0.06) 24px
        ),
        repeating-linear-gradient(
          0deg, 
          rgba(255, 255, 255, 0.1), 
          rgba(255, 255, 255, 0.1) 1px, 
          transparent 1px, 
          transparent 30px
        );
      opacity: 0.3;
      z-index: -1;
    }
    
    /* Main content */
    .container {
      width: 90%;
      max-width: 1200px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 40px;
      padding: 30px;
      position: relative;
    }
    
    /* Left side - Image/illustration */
    .left-side {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }
    
    .fish-container {
      width: 100%;
      max-width: 450px;
      height: 450px;
      border-radius: 50%;
      background: var(--bg-color);
      box-shadow: 20px 20px 40px var(--shadow-dark),
                 -20px -20px 40px var(--shadow-light);
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
    }
    
    .water-effect {
      position: absolute;
      width: 120%;
      height: 120%;
      background: linear-gradient(
        135deg,
        rgba(0, 255, 204, 0.1),
        rgba(42, 151, 201, 0.4),
        rgba(14, 92, 133, 0.3)
      );
      opacity: 0.4;
      top: -10%;
      left: -10%;
      border-radius: 50%;
      animation: water-move 15s infinite linear;
    }
    
    @keyframes water-move {
      0% {
        transform: rotate(0deg) scale(1);
      }
      50% {
        transform: rotate(180deg) scale(1.1);
      }
      100% {
        transform: rotate(360deg) scale(1);
      }
    }
    
    .fish-icon {
      font-size: 180px;
      color: var(--primary-color);
      position: relative;
      z-index: 1;
      text-shadow: 5px 5px 15px rgba(0, 0, 0, 0.1);
      animation: float 6s ease-in-out infinite;
    }
    
    @keyframes float {
      0%, 100% {
        transform: translateY(0px) rotate(0deg);
      }
      50% {
        transform: translateY(-20px) rotate(5deg);
      }
    }
    
    /* Small fish decorations */
    .small-fish {
      position: absolute;
      font-size: 25px;
      animation: small-fish-swim 15s linear infinite;
      opacity: 0.7;
      z-index: 0;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
    }
    
    .small-fish:nth-child(1) {
      top: 20%;
      left: -5%;
      animation-duration: 12s;
      animation-delay: 0s;
    }
    
    .small-fish:nth-child(2) {
      top: 50%;
      left: -5%;
      animation-duration: 18s;
      animation-delay: 3s;
    }
    
    .small-fish:nth-child(3) {
      top: 75%;
      left: -5%;
      animation-duration: 15s;
      animation-delay: 6s;
    }
    
    @keyframes small-fish-swim {
      0% {
        transform: translateX(0) scaleX(1);
      }
      49% {
        transform: translateX(110%) scaleX(1);
      }
      50% {
        transform: translateX(110%) scaleX(-1);
      }
      99% {
        transform: translateX(0) scaleX(-1);
      }
      100% {
        transform: translateX(0) scaleX(1);
      }
    }
    
    /* Right side - Content */
    .right-side {
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    
    .welcome-box {
      padding: 15px 30px;
      border-radius: 15px;
      background: var(--bg-color);
      box-shadow: 5px 5px 10px var(--shadow-dark),
                 -5px -5px 10px var(--shadow-light);
      margin-bottom: 30px;
      display: inline-block;
      align-self: flex-start;
    }
    
    .welcome-text {
      color: var(--primary-color);
      font-weight: 600;
      font-size: 1rem;
    }
    
    h1 {
      color: var(--text-color);
      font-size: 2.8rem;
      line-height: 1.3;
      margin-bottom: 20px;
      font-weight: 700;
    }
    
    .tagline {
      color: var(--text-color);
      opacity: 0.8;
      font-size: 1.1rem;
      line-height: 1.6;
      margin-bottom: 40px;
    }
    
    .highlight {
      color: var(--primary-color);
      font-weight: 500;
    }
    
    .buttons {
      display: flex;
      gap: 20px;
    }
    
    .btn {
      text-decoration: none;
      padding: 16px 40px;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      display: inline-block;
      letter-spacing: 0.5px;
    }
    
    .btn-primary {
      color: white;
      background: var(--primary-color);
      box-shadow: 5px 5px 10px var(--shadow-dark),
                -2px -2px 5px var(--shadow-light);
    }
    
    .btn-primary:hover {
      background: var(--secondary-color);
      transform: translateY(-3px);
      box-shadow: 7px 7px 15px var(--shadow-dark),
                 -2px -2px 5px var(--shadow-light);
    }
    
    .btn-secondary {
      color: var(--text-color);
      background: var(--bg-color);
      box-shadow: 5px 5px 10px var(--shadow-dark),
                 -5px -5px 10px var(--shadow-light);
    }
    
    .btn-secondary:hover {
      transform: translateY(-3px);
      box-shadow: 7px 7px 15px var(--shadow-dark),
                 -7px -7px 15px var(--shadow-light);
    }
    
    /* Circles decoration */
    .circle {
      position: absolute;
      border-radius: 50%;
      background: white;
      opacity: 0.1;
      z-index: -1;
    }
    
    .circle-1 {
      width: 300px;
      height: 300px;
      top: -150px;
      left: -150px;
    }
    
    .circle-2 {
      width: 200px;
      height: 200px;
      bottom: -100px;
      right: 10%;
    }
    
    /* Responsive styles */
    @media (max-width: 1100px) {
      .container {
        padding: 20px;
        gap: 30px;
      }
      
      .fish-container {
        height: 400px;
      }
      
      .fish-icon {
        font-size: 150px;
      }
      
      h1 {
        font-size: 2.5rem;
      }
      
      .tagline {
        font-size: 1rem;
      }
    }
    
    @media (max-width: 900px) {
      .container {
        grid-template-columns: 1fr;
        max-width: 600px;
        gap: 20px;
      }
      
      .left-side {
        order: 2;
      }
      
      .right-side {
        order: 1;
        text-align: center;
      }
      
      .welcome-box {
        align-self: center;
      }
      
      .fish-container {
        height: 300px;
        max-width: 300px;
      }
      
      .fish-icon {
        font-size: 120px;
      }
      
      h1 {
        font-size: 2.2rem;
      }
      
      .buttons {
        justify-content: center;
      }
    }
    
    @media (max-width: 600px) {
      .container {
        padding: 15px;
      }
      
      h1 {
        font-size: 1.8rem;
      }
      
      .tagline {
        font-size: 0.9rem;
        margin-bottom: 30px;
      }
      
      .btn {
        padding: 14px 30px;
        font-size: 0.9rem;
      }
      
      .fish-container {
        height: 250px;
        max-width: 250px;
      }
      
      .fish-icon {
        font-size: 100px;
      }
    }
    
    @media (max-width: 480px) {
      .buttons {
        flex-direction: column;
        gap: 15px;
      }
      
      .welcome-text {
        font-size: 0.9rem;
      }
      
      .fish-container {
        height: 200px;
        max-width: 200px;
      }
      
      .fish-icon {
        font-size: 80px;
      }
    }
  </style>
</head>
<body>
  <!-- Background elements -->
  <div class="background"></div>
  <div class="pattern"></div>
  <div class="circle circle-1"></div>
  <div class="circle circle-2"></div>
  
  <!-- Main content -->
  <div class="container">
    <!-- Left side - Fish illustration -->
    <div class="left-side">
      <div class="fish-container">
        <div class="water-effect"></div>
        <div class="small-fish">🐠</div>
        <div class="small-fish">🐟</div>
        <div class="small-fish">🐡</div>
        <div class="fish-icon">🐋</div>
      </div>
    </div>
    
    <!-- Right side - Content -->
    <div class="right-side">
      <div class="welcome-box">
        <p class="welcome-text">SMART FISH CARE SYSTEM</p>
      </div>
      <h1>Welcome to Smart Fish Care Management System</h1>
      <p class="tagline">
        Experience precision <span class="highlight">monitoring</span> and <span class="highlight">management</span> of your aquatic ecosystems for optimal fish health and productivity.
      </p>
      <div class="buttons">
        <a href="/login" class="btn btn-primary">Login</a>
        <a href="/signup" class="btn btn-secondary">Sign Up</a>
      </div>
    </div>
  </div>
</body>
</html> 